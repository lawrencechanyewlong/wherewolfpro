.container

  %h1 Summary
  %h3 Address
  %p= "#{@address_string}"
  - if @address_lat and @address_lng
    %p= "Lat: #{@address_lat}, Lng: #{@address_lng}"
  %h3 Contacts
  %p
    - if @receiver_name
      - (0...@receiver_name.size).each do |i|
        = "#{@receiver_name[i]} #{@receiver[i]}"
        - if i != @receiver_name.size - 1
          = ", "
  %h3 Duration
  %p= "#{@duration}"
  %h3 Message

  %p= "#{session[:message]}"
  %h4 Testing location
  %P= "#{session[:latlong]}"
  <!-- %p= "latlng: #{session[:latlng]}" -->
  
  %form{:id => "create", :action => "/event/summary", :method => "post"}
    %input#submit-form.hidden{:type => "submit"}/
  
  %nav.navbar.navbar-fixed-bottom{:style => "background: #000; padding: 20px"}
    .container
      .row
        .col-xs-3
          %a.btn.btn-danger{:href => "/event/messag"}
            %span.glyphicon.glyphicon-arrow-left{"aria-hidden" => "true"}
            %span Back
        .col-xs-6
        .col-xs-3
          %a.btn.btn-success{:href => "/event/send_mail"}
            %span Send 
            <!-- %span.glyphicon.glyphicon-arrow-right{"aria-hidden" => "true"} -->

